#### 100% 에서 틀렸습니다.. ####
- cin >> n
- vector<long long> v(n,0)
- for i=0 to n-1:
	- cin >> v[i]
- sort(v.begin(), v.end())
- cnt = 1, max = 1
- vector<long long> val
- for i=0 to n-2:
	- if v[i]==v[i+1]: cnt += 1
	- else:
		- if cnt > max:
			- val.clear(), val.push_back(v[i])
		- else if cnt==max:
			- val.push_back(v[i])
		- max = cnt, cnt = 1
- if cnt > max: result = v[n-1]
- else: result = val[0]
- result 출력