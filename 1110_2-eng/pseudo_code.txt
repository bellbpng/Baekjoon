- input N
- init originNum
	- if N<10 then:
		- paste '0' before N and then change the number into a string
	- else then:
		- change N into a string
- init pastNum = originNum
- init newNum = ""
- while originNum != newNum do:
	- init leftNum in a integer getting pastNum[0]
	- init rightNum in a integer getting pastNum[1]
	- temp = leftNum + rightNum
	- revise the variable newNum to get the string in a form, "pastNum[1]+unit of temp"
	- cnt += 1
	- pastNum = newNum