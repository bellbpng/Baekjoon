- 단어의 개수 N 을 입력받는다.
- vector 컨테이너에 N개의 문자열을 입력 받아 담는다.
	- for i=0 to N-1 do:
		- v.push_back(string)
- <algorithm>에 있는 sort() 함수를 활용한다.
	- 비교함수를 직접 작성해야 한다.
	- bool comp(string1, string2)
		- if length of string1 != length of string2 then: return (string1.length()<string2.length())
		- else then: return (string1<string2)
- sort() 함수를 호출하여 vector 컨테이너 v를 정렬한다.
- 정렬이 완료되면 v의 모든 원소를 출력한다.
	- 같은 단어가 여러번 입력된 경우 한번만 출력한다.