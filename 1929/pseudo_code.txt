- M과 N이 공백을 사이에 두고 입력된다.
- 소수임을 확인할 수 있는 함수를 만든다. 
- function checkPrime(number) do:
	- if 1 then false
	- else if number가 2 or 3 then true
	- else if number가 2의 배수 then false
	- for i=3 to (sqrt of number) do:
		- if number%i 가 0 then false
	- 반복문을 끝까지 완료하면 true
- for i=M to N do:
	- if checkPrime(i)가 true then i 출력

