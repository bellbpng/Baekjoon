- cin >> t
- for i=0 to t-1:
	- cin >> n >> m
	- sum = m*(m+1)/2 #sum of all integers between 1 and m
	- if n<0: sum -= abs(n)*(abs(n)+1)/2 #sum of all integers between -1 and n
	- else if n>1: sum -= (n-1)*n/2