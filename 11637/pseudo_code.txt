- cin >> t
- for i=0 to t-1:
	- cin >> n
	- vector<int> votes(n)
	- total = 0
	- max = 1, idx = 1
	- for j=0 to n-1:
		- cin >> votes[j]
		- total += votes[j]
		- if max < votes[j]: 
			- max = votes[j], idx = j+1
	- cnt = 0
	- for j=0 to n-1:
		- if max==votes[j]: cnt+=1
	- if cnt>1: "no winner" 
	- else if max > total/2: "majority winner"
	- else: "minority winner"